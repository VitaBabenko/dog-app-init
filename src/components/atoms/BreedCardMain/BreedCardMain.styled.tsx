import { Button, styled } from '@mui/material';

export const StyledButton = styled(Button)({
  display: 'flex',
  gap: '10px',
  position: 'absolute',
  bottom: '10px',
  left: '50%',
  transform: 'translate(-50%)',
  width: '245px',
  padding: '10px 20px'
});
